name: go_lance
description: "A new Flutter project."
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.3.1 <=4.0.0"

dependencies:
  cupertino_icons: ^1.0.6
  flutter:
    sdk: flutter
  google_fonts: ^6.2.1
  objectbox: ^2.5.1
  objectbox_sync_flutter_libs: ^2.5.1
  path: ^1.9.0

dev_dependencies:
  flutter_lints: ^3.0.0
  flutter_test:
    sdk: flutter
  objectbox_generator: ^2.5.1
  build_runner: ^2.4.8

flutter:
  uses-material-design: true
  assets:
    - assets/
